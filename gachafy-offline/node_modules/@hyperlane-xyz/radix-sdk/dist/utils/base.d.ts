import { GatewayApiClient } from '@radixdlt/babylon-gateway-api-sdk';
import { TransactionHash, TransactionManifest, Value } from '@radixdlt/radix-engine-toolkit';
export declare class RadixBase {
    protected networkId: number;
    protected gateway: GatewayApiClient;
    protected gasMultiplier: number;
    constructor(networkId: number, gateway: GatewayApiClient, gasMultiplier: number);
    getXrdAddress(): Promise<string>;
    isGatewayHealthy(): Promise<boolean>;
    estimateTransactionFee({ transactionManifest, }: {
        transactionManifest: TransactionManifest | string;
    }): Promise<{
        gasUnits: bigint;
        gasPrice: number;
        fee: bigint;
    }>;
    getMetadata({ resource }: {
        resource: string;
    }): Promise<{
        name: string;
        symbol: string;
        description: string;
        divisibility: number;
    }>;
    getXrdMetadata(): Promise<{
        name: string;
        symbol: string;
        description: string;
        divisibility: number;
    }>;
    getBalance({ address, resource, }: {
        address: string;
        resource: string;
    }): Promise<bigint>;
    getXrdBalance({ address, }: {
        address: string;
    }): Promise<bigint>;
    getTotalSupply({ resource, }: {
        resource: string;
    }): Promise<bigint>;
    getXrdTotalSupply(): Promise<bigint>;
    pollForCommit(intentHashTransactionId: string): Promise<void>;
    getNewComponent(transaction: TransactionHash): Promise<string>;
    createCallFunctionManifest(from_address: string, package_address: string | number, blueprint_name: string, function_name: string, args: Value[]): Promise<TransactionManifest>;
    createCallMethodManifestWithOwner(from_address: string, contract_address: string, method_name: string, args: Value[]): Promise<TransactionManifest>;
    transfer({ from_address, to_address, resource_address, amount, }: {
        from_address: string;
        to_address: string;
        resource_address: string;
        amount: string;
    }): Promise<TransactionManifest>;
}
//# sourceMappingURL=base.d.ts.map